{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories":{
    "test": "tests"
  },
  "scripts": {
    "api-test-production": "newman run tests/LearnAPITesting.postman_collection.json --disable-unicode --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "",
  "dependencies": {
    "newman": "^4.5.4"
  }
}
